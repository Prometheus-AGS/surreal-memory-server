[build]
# This repo is developed primarily on a 10-core Apple Silicon machine.
# Keep Cargo parallelism aligned with available local CPU cores.
jobs = 10

[env]
SCCACHE_CACHE_SIZE = "20G"
SCCACHE_IDLE_TIMEOUT = "0"
