## 1. Deterministic lookup

- [x] 1.1 Replace field-filtered receipt reads with deterministic record selection in both public and internal operation reads; verify the crate compiles

## 2. Verification

- [x] 2.1 Run the operations test targets and strict OpenSpec validation
- [x] 2.2 Install the rebuilt server and measure receipt lookup plus learning-worker backlog progress
